{
  "name": "zuar-portal-mcp-server",
  "version": "2.3.0",
  "description": "MCP server that lets Claude create and manage Zuar Portal blocks, pages, data sources, queries, users and more.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "zuar-portal-mcp-server": "dist/index.js"
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "test": "node --test dist-test/*.test.js",
    "prepublishOnly": "npm run build"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^3.25.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0"
  }
}

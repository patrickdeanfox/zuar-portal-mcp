node_modules/
dist/
dist-test/
*.mcpb
config.json
# Per-project portal credentials (multi-portal config) — never commit.
.zuar-portal/
.zuar-portal.json
.DS_Store
test-harness.mjs
# Real portal block exports — may contain customer data; keep out of the repo
portal_block_*.json
